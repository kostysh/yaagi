# Demo Skill

This is the minimal repository-owned demo skill used to verify the canonical
`seed/skills -> workspace/skills -> validator -> loader` runtime path.

## Usage

- Treat this package as a proof artifact for `F-0022`.
- Keep the skill centered on `SKILL.md` plus bounded support subtrees.
- Use the companion reference file for deterministic loading checks.
